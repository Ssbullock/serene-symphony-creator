import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Meditations.serene',
  appName: 'Serene Meditations',
  webDir: 'dist'
};

export default config;
